<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - ActivityHub.Presantaion</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/daisyui@1.16.0/dist/full.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Chakra+Petch:wght@400;700&display=swap" rel="stylesheet"> <!-- Google Fonts -->
    <style>
        body {
            background-color: #2F5B4C;
            font-family: 'Chakra Petch', sans-serif;
            background-image: url('https://images.pexels.com/photos/834977/pexels-photo-834977.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=2'); /* Arka plan görseli URL'sini buraya ekleyin */
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .navbar, .footer {
            background-color: #19362C;
            color: white;
            padding: 1rem 2rem;
        }

        .btn {
            background-color: #487355;
            color: white;
            padding: 0.75rem 1.5rem;
            font-size: 1.1rem;
            border-radius: 0.5rem;
        }

            .btn:hover {
                background-color: #3B5F48;
            }

        .content {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        .form-container {
            background-color: rgba(224, 247, 233, 0.8); /* Çok açık yeşil ve şeffaf */
            padding: 2rem;
            border-radius: 0.5rem;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .footer {
            margin-top: auto;
            text-align: center;
        }

        .shadow-text {
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }
    </style>
</head>
<body>
    <header class="navbar">
        <nav class="container mx-auto flex justify-between items-center">
            <a class="text-white text-2xl font-bold" href="/">ActivityHub</a>
        </nav>
    </header>

    <main class="content">
        @RenderBody()
    </main>

    <footer class="footer p-4">
        <span>ActivityHub</span>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.0/dist/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
